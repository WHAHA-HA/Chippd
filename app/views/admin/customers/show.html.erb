<%= ariane.render %>

<h2><%= resource.full_name %>'s Pages</h2>
<ul>
<% resource.pages.each do |page| %>
  <li><%= link_to(page.name, admin_page_path(page)) %></li>
<% end %>
</ul>

<h2>Memberships</h2>
<%= simple_table_for resource.memberships, :with => "MembershipTable", :present_with => CrushTablePresenter %>

<hr />
