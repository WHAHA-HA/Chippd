<%= form.input :name, :hint => "The human-readable name of the discount. e.g. 50% Off For You!" %>
<%= form.input :code, :hint => raw("The code a customer will enter to apply this discount to their order. It's recommended that you use an all-caps alphanumeric string with no spaces. e.g. <code>50OFF4U</code>") %>
